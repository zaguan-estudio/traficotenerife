name            = "traficotenerife"
main            = "worker.js"
compatibility_date = "2024-09-23"

# Secrets (nunca van aquí — se añaden con `wrangler secret put GEMINI_API_KEY`)
# Variables públicas opcionales:
# [vars]
# ALGUNA_VAR = "valor"
